{
  "name": "hyperdrive-encoding",
  "version": "2.0.1",
  "license": "MIT",
  "repository": "juliangruber/hyperdrive-encoding",
  "description": "Message encoding used by hyperdrive",
  "devDependencies": {
    "standard": "^8.2.0",
    "tap": "^8.0.0"
  },
  "scripts": {
    "test": "tap test.js && standard"
  },
  "browserify": {
    "transform": [
      "brfs"
    ]
  },
  "dependencies": {
    "protocol-buffers": "^3.2.1",
    "brfs": "^1.4.3"
  }
}
